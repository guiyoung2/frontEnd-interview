# frontend-interview

Notion API 기반 프론트엔드 면접 질문 정리/조회 프로젝트입니다.

## 실행

```bash
pnpm dev
```

브라우저에서 `http://localhost:3000`을 열어 확인합니다.

## 핵심 원칙

- Next.js App Router 사용
- 스타일은 Vanilla Extract(`.css.ts`)만 사용
- Tailwind CSS, Emotion 사용 금지
